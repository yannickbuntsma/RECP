import * as React from 'react'
import styled from '@emotion/styled'

export type Props = {}

const RecipeCardDetails: React.FC<Props> = ({ children }) => {
  return <div>{children}</div>
}

export default styled(RecipeCardDetails)``

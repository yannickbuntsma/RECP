import { getUnitLabel } from './get-unit-label'

const mockLocale = {}

describe('getUnitLabel', () => {
  it('should return the correct singlular/plural label based on amount', () => {
    //
  })
})

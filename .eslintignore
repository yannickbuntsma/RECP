# don't ever lint node_modules
node_modules

# don't lint build output
.next
.now
.storybook
public

# don't lint nyc coverage output
coverage

# don't lint config files
*.config.js

# don't lint type def files
*.d.ts
